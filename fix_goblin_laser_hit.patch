--- src/game/game_struct.rs
+++ src/game/game_struct.rs
@@ -5855,6 +5855,10 @@
                 if let Some((x, y, text, color)) = damage_text {
                     self.spawn_floating_text(x, y, text, color);
                 }
+
+                if let Some(ref gob) = self.goblin {
+                    println!("Laser pos: {:?}, laser player: {}, gob pos: {:?}", laser.position, laser.player, gob.position);
+                }
                 if let Some(gob_pos) = self.goblin.as_ref().map(|g| g.position).filter(|p| *p == laser.position) {
                     self.goblin = None;
                     if !is_piercing {
