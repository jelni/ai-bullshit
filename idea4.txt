Wait, let's implement the Tron mode properly.
Wait, Tron Mode:
In `GameMode::Tron`, all snakes leave a trail that does not shrink.
So `p1_grow` and `p2_grow` and `bots_grow` are ALWAYS true.
Also, the snakes constantly sprint? In Tron, lightcycles move constantly and fast.
Let's add GameMode::Tron.
But is that large and meaningful? It's just a 3 line change.
"think what would be the best next meaningful, large, major development"

What about a real **"Replay & Spectator System"**?
Right now, `ghost.json` only saves for `Speedrun`. What if we record EVERYTHING and allow the user to play back any game from the menu "Replays"?
Or what about a **"Level Editor"**?
Wait, `GameState::LevelEditor` already exists!
Let's see if we can do **"Map Generation / Biomes"**.
Or **"Snake Weapon Loadouts"**?
Or **"Multiplayer Server"**? No, that requires sockets, maybe too complex.

What about a **"Campaign Mode Expansion"**?
`campaign_maps.rs` has 3.9 MB of data?! Wait, `ls -l src/game/` showed `campaign_maps.rs` is 3.9 MB!
Let's look at `campaign_maps.rs`.
