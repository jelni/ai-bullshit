1. **Update A* Heuristic in `src/game.rs`**
   - Add a penalty to the A* heuristic logic for points near a goblin, ensuring the bot will avoid the goblin when not specifically targeting it.
2. **Pre Commit Steps**
   - Ensure proper testing, verification, review, and reflection are done by calling the `pre_commit_instructions` tool and executing its steps.
3. **Submit Changes**
   - Commit and submit the code.
