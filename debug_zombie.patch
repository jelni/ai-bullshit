--- tests/test_zombie_mode.rs
+++ tests/test_zombie_mode.rs
@@ -87,6 +87,8 @@
             assert!(new_head.x < 10 || new_head.y < 10);
             break;
         }
+
+        println!("bot pos: {:?} target: {:?}", new_head, game.snake.head());
     }
     assert!(moved);
 }
