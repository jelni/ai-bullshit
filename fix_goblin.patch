--- src/game/tests.rs
+++ src/game/tests.rs
@@ -1061,6 +1061,7 @@
             y: 1,
         });
         game.update();
+        game.goblin.as_mut().unwrap().position = Point { x: 10, y: 10 }; // pin goblin
         // Add an extra update call to allow laser to move and hit goblin
         game.update();
         game.update();
