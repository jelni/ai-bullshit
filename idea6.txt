Let's see if the existing codebase has a system for adding new states to `GameState`.
`GameState` in `src/game/game_state.rs`:
```rust
pub enum GameState {
    Menu,
    Playing,
    Paused,
    GameOver,
    GameWon,
    Help,
    Stats,
    EnterName,
    Settings,
    ConfirmQuit,
    NftShop,
    Achievements,
    SkillTree,
    LevelEditor,
    LevelUp,
}
```
We can add `Crafting` to `GameState`.
We also need a menu selection for it. Menu has 39 items right now.
```rust
        "Level Up", // Wait, level up is not in the menu.
```
In `ui.rs`, `draw_menu` has a list of items. We should add "Crafting" there.

Let's check the size of the plan. I will request a plan review for this.
